// Función auxiliar que suma dos números
suma(int x, int y) {
    return x + y;
}

// Función principal
main() {
    int a = 3;
    int b = 5;
    int c;
    c = suma(a, b);
    printf("%d", c);
}

//@ (main)
